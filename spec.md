# ShareAZ 仕様書

**バージョン**: v1.4
**作成日**: 2026-04-24
**更新日**: 2026-04-26
**ステータス**: フェーズ1 ほぼ完成（ポイント交換機能まで実装。Firebase Storage / GitHub 連携 / 本番セキュリティルールが残課題）

---

## 1. プロダクト概要

### アプリ名
**ShareAZ**

### コンセプト
社員が得意スキルや趣味・関心をシェアし、共通の興味を持つ仲間とつながれる社内コミュニティSNS。  
イベント参加・主催・貢献で「AZコイン」を獲得でき、社内オリジナルノベルティと交換できる報酬システムも持つ。

### ターゲットユーザー
- 全社員（個人アカウントを持ち、スキル・趣味を発信したい人）
- イベントを企画・告知したい社員
- 社内の横のつながりを増やしたい社員

### 想定規模
- 初期：少人数（〜50人）
- 将来：1,000人以上

### 目標
- フェーズ1：基本SNS機能（プロフィール・タグ検索・掲示板）+ AZコイン・ポイント交換 → ほぼ完成
- フェーズ2：イベント作成承認制 / データ集計 / 通知
- フェーズ3：スケール対応（1,000人以上）

---

## 2. 機能定義

### 2-1. ユーザー管理

| # | 項目 | 説明 |
|---|------|------|
| 1 | 招待制登録 | 管理者が社員のメールアドレスを招待リンク付きで登録する |
| 2 | メール＋パスワード認証 | Firebase Authentication を利用 |
| 3 | プロフィール設定 | 氏名・所属部署・自己紹介・アイコン画像・タグ |
| 4 | ロール | `admin`（管理者） / `member`（一般社員）の2種 |

### 2-2. プロフィール

| # | 項目 | 入力形式 | 必須 | 説明 |
|---|------|----------|------|------|
| 1 | 氏名 | テキスト | 必須 | 表示名 |
| 2 | 所属部署 | テキスト | 任意 | チーム・部署名 |
| 3 | 自己紹介 | テキスト（長文） | 任意 | 自由記述 |
| 4 | アイコン画像 | 画像アップロード | 任意 | Firebase Storage 保存（未実装：次フェーズ） |
| 5 | タグ | 複数選択（自由入力） | 任意 | 例：「ゲーム」「デザイン」「料理」 |
| 6 | AZコイン残高 | 数値（表示のみ） | - | 自動更新 |

### 2-3. メンバー検索・お気に入り

#### フェーズ1（MVP）

| # | 項目 | 説明 |
|---|------|------|
| 1 | タグ検索 | 指定タグを持つメンバー一覧を表示 |
| 2 | 名前検索 | 氏名の部分一致検索 |
| 3 | タグ一覧 | 使われているタグの一覧（クリックで絞り込み） |
| 4 | お気に入りメンバー | 個人ごとに「お気に入り」リストを保持（自分専用、相互不要） |

#### フェーズ2（将来拡張・MVPでは未実装）

| # | 項目 | 説明 |
|---|------|------|
| 5 | 共参加回数表示 | 「○○さんと過去3回一緒のイベントに参加」を表示 |
| 6 | チーム機能（部活） | 共通の興味でチーム作成・チーム単位の活動 |

### 2-4. 掲示板（告知・イベント）

#### カテゴリ（2種類）

| カテゴリ | 用途 | 主な機能 |
|---------|------|---------|
| `announcement`（告知） | 「こんなイベントやりたい！」と仲間を募る段階 | 「興味あり」リアクション + コメント |
| `event`（イベント） | 開催が確定したイベント | 参加申請 + 参加者一覧 + コメント |

#### 告知 → イベント の作成フロー

```
[1] 告知スレッド作成「料理交流会やりたい人いませんか？」
    ↓
[2] コメント欄で議論／「興味あり」リアクションが集まる
    ↓
[3] 作成者が「イベントを作成する」ボタンをタップ
    ↓ 開催日時・定員を入力
[4] イベントスレッドに変換され、参加申請ボタンが有効化
    ↓
[5] イベント開催 → 完了処理でAZコイン付与
```

- 一度作成したら **告知には戻せない**（操作ミス防止のため確認ダイアログを表示）
- 内部記録として `promotedFrom: "announcement"` をデータに保持（告知から作成されたイベントの判定用）

> **【将来仕様】イベント作成は管理者承認制にする予定**
>
> 現状は告知の作成者がワンタップでイベントを作成できますが、将来的には以下のフローに変更予定：
> 1. 作成者が「イベントを作成する」ボタンをタップ → **イベント作成申請** が管理者に送られる
> 2. 管理者が管理画面で確認・承認
> 3. 承認後にイベントスレッドが公開される
>
> 同様に、新規スレッド作成時に「イベント」カテゴリで投稿する場合も、将来は申請→承認フローを通る。
> これは社内SNSとしての品質管理（不適切なイベントの抑止）と、開催情報の整合性を担保するため。
> データ構造は `eventCreationRequests/` コレクションを新設して、`deletionRequests/` と同じパターンで実装予定。

#### スレッド項目

| # | 項目 | 入力形式 | 必須 | 説明 |
|---|------|----------|------|------|
| 1 | カテゴリ | 選択 | 必須 | `announcement` / `event`（デフォルト：告知） |
| 2 | タイトル | テキスト（最大100文字） | 必須 | スレッド名 |
| 3 | 本文 | テキスト（最大5000文字） | 必須 | 詳細・場所など |
| 4 | タグ | 複数入力（自由） | 任意 | 検索・絞り込み用（例：「料理」「ゲーム」） |
| 5 | 開催日時 | 日時入力 | 任意 | イベントカテゴリのみ |
| 6 | 定員 | 数値（0=無制限） | 任意 | イベントカテゴリのみ |

#### スレッド内コメント機能

- **誰でも投稿可**（認証ユーザー全員）
- 表示順：**古い順（議論の流れがわかる）**
- イベントスレッドのカードには 💬 コメント数バッジを表示
- **MVP では投稿のみ**（編集・削除は フェーズ2 で対応予定）

#### スレッド編集・削除

| 操作 | 権限 | 備考 |
|------|------|------|
| 編集（タイトル・本文・タグ） | 作成者本人 | 詳細画面の「編集」ボタンから |
| 削除 | 直接削除は不可 | 作成者は **「削除申請」** を出せる。管理者が承認すると削除される |
| 強制削除 | 管理者のみ | 不適切なスレッドを管理画面から削除 |

**削除申請の流れ：**
1. 作成者が詳細画面の「削除申請」ボタンをタップ
2. 削除理由を入力（任意）
3. `deletionRequests/` コレクションに申請が記録される
4. 管理者が管理画面で確認 → 承認すれば削除実行 / 却下すれば申請取り消し

#### 「興味あり」リアクション（告知のみ）

- 1ユーザー1回タップでリアクション、再タップで取り消し
- カードに `✋ 興味あり N` で件数表示
- 作成者が「イベントに昇華するか」の判断材料にする

#### 参加申請（イベントのみ）

| 項目 | 内容 |
|------|------|
| 参加申請ボタン | 1クリックで参加申請。再タップでキャンセル |
| 参加者一覧 | 申請したメンバーの名前・アイコンを表示（リアルタイム） |
| 主催者表示 | スレッド作成者を主催者として表示 |
| 定員到達時 | ボタンが「満員」表示（参加申請不可） |

### 2-5. AZコイン（ポイント）

| # | 付与条件 | 付与量 | 説明 |
|---|----------|--------|------|
| 1 | イベントに参加 | +10コイン | 参加申請したイベントが終了後に付与 |
| 2 | イベントを主催 | +30コイン | 自分が作成したスレッドが「完了」になると付与 |
| 3 | スキル・知識で貢献 | 任意（正負両対応） | 管理者が手動で付与 |
| 4 | 初回プロフィール設定 | +5コイン | タグを3つ以上設定で付与（自動付与は将来実装） |
| 5 | ポイント交換申請 | -coinCost | 申請時に即減算（writeBatch でアトミック） |
| 6 | 交換申請の却下 | +coinCost | 管理者が却下した場合のみ自動返金 |

> ※ コイン付与量は管理者が変更可能とする（将来対応）

### 2-6. ポイント交換（ノベルティ交換ショップ）

> v7 で本実装完了。旧称「交換ショップ」をユーザー向けには「ポイント交換」と表示。

#### 機能一覧

| # | 項目 | 説明 |
|---|------|------|
| 1 | ノベルティ一覧 | 公開中の商品を新着順表示（在庫切れはグレーアウト） |
| 2 | 交換申請 | コインを消費してアイテムに申請。**申請時に即減算** |
| 3 | 確認モーダル | 申請前に注意事項を表示（取り消し不可・即減算） |
| 4 | 申請履歴 | 自分の交換申請履歴（マイページ：申請中 / 処理済み の2セクション） |
| 5 | 在庫管理 | 管理者が在庫数を設定・管理（管理画面） |
| 6 | 公開トグル | 商品単位で表示／非表示（`isActive`） |

#### 動作仕様（確定）

| 項目 | 仕様 |
|------|------|
| コイン・在庫の減算タイミング | 申請時に即減算（writeBatch でアトミック） |
| 申請取り消し | ユーザー側からは不可（申請前に確認モーダルで注意喚起） |
| 却下処理 | 管理者却下のみで返金 + 在庫戻し（無制限商品 stock=-1 は戻し不要） |
| 商品画像 | 当面は外部URL手入力（Firebase Storage 有効化後に切替予定） |
| 在庫無制限 | `stock = -1` で表現、UI上は「在庫: ∞」表示 |
| 商品の非公開 | `isActive: false` でユーザー一覧から除外 |
| 商品削除 | 既存の交換申請レコードは残る（itemName/itemImageUrl/coinCost を非正規化保持） |

#### 申請時のアトミック処理（writeBatch）

1. `exchangeRequests/{auto-id}` 新規作成（`status: 'pending'`）
2. `users/{uid}.azCoins` を `increment(-coinCost)`
3. `shopItems/{itemId}.stock` を `increment(-1)`（無制限商品はスキップ）
4. `coinTransactions/` に `amount: -coinCost, reason: '🛍️「商品名」を交換申請'` を記録

#### 却下時のアトミック処理（writeBatch）

1. `exchangeRequests/{id}` を `status: 'rejected'` に更新（理由オプション）
2. `users/{uid}.azCoins` を `increment(+coinCost)`
3. `shopItems/{itemId}.stock` を `increment(+1)`（無制限商品はスキップ）
4. `coinTransactions/` に `amount: +coinCost, reason: '🛍️「商品名」交換却下による返金'` を記録

### 2-7. 管理者（Admin）機能

通常画面とは独立した **管理者専用画面** を別途実装する（一般ユーザーには非表示）。

#### 管理画面のタブ構成（v7 時点・7タブ）

4列×自動折り返しグリッドで配置（モバイル幅で全タブ一望）。

| # | タブ | アイコン | 機能 |
|---|------|---------|------|
| 1 | お知らせ | 📢 | 公式お知らせ投稿 / 既存スレッドの公式化トグル |
| 2 | イベント完了 | ✅ | イベントを「完了」処理 → 参加者+10、主催者+30 自動付与 |
| 3 | 削除申請 | 🗑️ | スレッド削除申請の承認 / 却下（理由付き） |
| 4 | コイン付与 | 🪙 | 任意ユーザーへのコイン手動付与（正負両対応・履歴表示） |
| 5 | ユーザー | 👥 | ユーザー管理（ロール変更・名前/所属フィルタ・自己ロック防止） |
| 6 | ノベルティ | 🛍️ | 商品の追加・編集・公開トグル・削除（v7 追加） |
| 7 | 交換申請 | 🎁 | 交換申請の承認 / 却下＋自動返金（v7 追加） |

#### 機能の詳細

| # | 機能 | 説明 |
|---|------|------|
| 1 | お知らせ投稿 | 管理画面から投稿したものは「公式お知らせ」として全画面で優先表示 |
| 2 | イベント完了処理 | イベント終了後に「完了」ボタン → 参加者+10、主催者+30、`eventCount/hostCount` 増加 |
| 3 | 削除申請の承認/却下 | サブコレクション完全削除（comments/interested/participations）+ 理由付き却下 |
| 4 | AZコイン手動付与 | 正負両対応・直近10件 + 全履歴モーダル |
| 5 | ユーザー管理 | ロール変更・名前/所属フィルタ・自己ロック防止 |
| 6 | ノベルティ管理 | 商品の追加・編集・公開トグル・削除（在庫設定含む） |
| 7 | 交換申請承認/却下 | 承認はステータス更新のみ、却下時は writeBatch で自動返金＋在庫戻し |
| — | ユーザー招待 | 未実装（フェーズ2）：メールアドレス招待 |
| — | データ集計 | 未実装（フェーズ2）：コイン履歴・参加率・人気タグ統計、CSV出力 |

### 2-8. メンバーランク機能

イベント参加・主催の累計実績によって、自動でランクが上がる仕組み。プロフィール画面にバッジ表示。

| ランク | 条件 | バッジ |
|--------|------|--------|
| ブロンズ | 0〜4回参加 | 🥉 |
| シルバー | 5〜14回参加 | 🥈 |
| ゴールド | 15回以上参加 | 🥇 |
| マスター | ゴールド条件 **かつ** 主催3回以上 | 👑 |

- 「参加」のカウント：イベントが「完了」になった時点で +1（`users.eventCount`）
- 「主催」のカウント：自分が作成したイベントが「完了」になった時点で +1（`users.hostCount`）
- ランクは表示時に動的計算（`getMemberRank(profile)` 関数）

---

## 3. 画面構成

### 3-1. 画面一覧

#### ボトムナビゲーション順序（左→右）

**ホーム → 掲示板 → メンバー → ポイント交換 → マイページ**

> v7 で「ショップ」→「ポイント交換」にリネーム。

#### 全画面リスト

| 画面名 | 種別 | 役割 |
|--------|------|------|
| ログイン画面 | 起動時 | メール・パスワード認証 |
| ホーム | メイン | 公式お知らせ + 最新スレッドを優先表示 |
| 掲示板 | メイン | 告知・イベント一覧（タグ絞り込み） |
| スレッド詳細画面 | 遷移 | 本文・コメント欄・参加申請/興味あり・昇華ボタン |
| 新規スレッド作成画面 | 遷移 | 告知/イベントの作成 |
| メンバー検索画面 | メイン | タグ・名前でメンバーを探す + お気に入り |
| プロフィール画面（他者） | 遷移 | 他のメンバーのプロフィール閲覧 |
| ポイント交換 | メイン | AZコインで交換できるノベルティ一覧 + 確認モーダル |
| マイページ（自分） | メイン | プロフィール・コイン残高・参加イベント履歴・**交換申請履歴**・ランクバッジ |
| 管理画面（Admin専用） | 遷移 | 7タブ：お知らせ / イベント完了 / 削除申請 / コイン付与 / ユーザー / ノベルティ / 交換申請 |

### 3-2. 各画面の詳細

#### ホーム
- 上部：**公式お知らせ（管理画面から投稿された Admin の告知）** を最優先表示
- 下部：通常スレッド（告知・イベント）を新着順
- スレッドカード：タイトル・カテゴリ・主催者・タグ・開催日・参加人数 / コメント数 / 興味あり数
- タップでスレッド詳細へ遷移

#### 掲示板
- 上部にタグ絞り込み（**「タグで絞り込む」ボタン → ボトムシート方式**）
- スレッド一覧（新着順）
- 右下にFAB（＋ボタン）で新規作成

#### メンバー検索画面
- 上部にタグ一覧（タップで絞り込み）
- テキスト検索ボックス（名前検索）
- 検索結果：メンバーカード（アイコン・名前・タグ一覧・ランクバッジ）
- カードタップでプロフィール詳細へ遷移

#### マイページ（自分）
- アイコン・氏名・部署・自己紹介
- 所持タグの一覧（編集ボタンで追加・削除）
- AZコイン残高（ヘッダー右上に常時表示）
- **ランクバッジ**（🥉🥈🥇👑、参加・主催回数で自動判定）
- **参加イベント履歴**（開催前 / 開催後 でセクション分け、新着順）
- **主催イベント履歴**（自分が作成したイベント）
- **交換申請履歴**（申請中 / 処理済み の2セクション）← v7 追加
- ログアウトボタン
- 管理者は「管理画面へ」ボタン

#### スレッド詳細画面
- ヘッダー：「←」戻るボタン + カテゴリバッジ
- タイトル・本文・タグ・主催者情報
- イベントの場合：開催日時・定員・参加申請ボタン・参加者一覧
- 告知の場合：「✋ 興味あり」ボタン + 件数 + 「イベントに昇華する」ボタン（作成者のみ表示）
- 下部：コメント一覧（古い順）+ コメント投稿フォーム

#### ポイント交換画面（ShopScreen）
- 公開中の商品を縦スクロールカード一覧で表示（新着順）
- 各カード：画像（外部URL）・名前・説明・必要コイン数・在庫表示・「交換する」ボタン
- 在庫切れ：カードがグレーアウト＋「在庫切れ」バッジ
- コイン不足：ボタンが「あと N 必要」表示でグレーアウト
- 「交換する」→ **確認モーダル**（注意喚起：取り消し不可・即減算）→ 申請実行
- 申請成功時：成功メッセージを表示

#### 管理画面（Admin専用）
- 一般のボトムナビには表示しない（マイページから「管理画面へ」ボタンで遷移）
- **4列×2行のグリッドタブ**（v7 でUX改善）：横スクロール廃止、全タブ一望
- 各タブはアイコン上＋ラベル下の縦組みコンパクト表示

---

## 4. データ設計（Firebase Firestore）

> **補足**：Firestore は「コレクション（collection）」と「ドキュメント（document）」でデータを管理します。  
> イメージとしては、コレクション＝引き出し、ドキュメント＝引き出しの中の1枚のメモです。

### コレクション構成

```
Firestore
├── users/                         ← ユーザープロフィール
│   └── {userId}/
│       ├── name: string
│       ├── department: string
│       ├── bio: string
│       ├── avatarUrl: string
│       ├── role: "admin" | "member"
│       ├── azCoins: number
│       ├── tags: string[]         ← タグは配列で持つ（例：["ゲーム", "料理"]）
│       ├── eventCount: number     ← 完了イベントへの参加累計（ランク判定用）
│       ├── hostCount: number      ← 完了イベントの主催累計（ランク判定用）
│       ├── favoriteUserIds: string[] ← お気に入りメンバーのuserId配列（自分専用）
│       ├── createdAt: timestamp
│       └── updatedAt: timestamp
│
├── tags/                          ← タグマスタ（全タグの一覧 / オートコンプリート用）
│   └── {tagId}/
│       ├── name: string           ← タグ名（例："ゲーム"）
│       └── createdAt: timestamp
│
├── threads/                       ← 掲示板スレッド（告知・イベント）
│   └── {threadId}/
│       ├── authorId: string
│       ├── authorName: string     ← 非正規化、表示高速化
│       ├── title: string
│       ├── body: string
│       ├── category: "announcement" | "event"
│       ├── tags: string[]
│       ├── eventDate: timestamp | null  ← イベントカテゴリのみ
│       ├── capacity: number       ← 0=無制限、イベントのみ
│       ├── status: "open" | "closed" | "completed"
│       ├── participantCount: number
│       ├── commentCount: number
│       ├── interestedCount: number
│       ├── promotedFrom: "announcement" | null
│       ├── isOfficial: boolean
│       ├── createdAt: timestamp
│       ├── updatedAt: timestamp
│       ├── participations/        ← サブコレクション（参加申請、イベントのみ）
│       │   └── {userId}/
│       │       ├── userId: string
│       │       ├── status: "pending" | "approved" | "rejected"
│       │       └── createdAt: timestamp
│       ├── comments/              ← サブコレクション
│       │   └── {commentId}/
│       │       ├── authorId, authorName, body
│       │       └── createdAt: timestamp
│       └── interested/            ← サブコレクション（告知のみ）
│           └── {userId}/
│               └── createdAt: timestamp
│
├── deletionRequests/              ← スレッド削除申請
│   └── {requestId}/
│       ├── threadId, threadTitle
│       ├── requesterId, requesterName
│       ├── reason: string
│       ├── status: "pending" | "approved" | "rejected"
│       ├── rejectionReason: string  ← 却下時のみ
│       ├── processedBy, processedAt
│       └── createdAt: timestamp
│
├── coinTransactions/              ← AZコイン異動履歴
│   └── {transactionId}/
│       ├── userId: string
│       ├── userName: string         ← 非正規化、表示用
│       ├── amount: number           ← プラスで付与、マイナスで消費
│       ├── reason: string           ← 'イベント参加' | 'イベント主催' | '🛍️「…」を交換申請' | '🛍️「…」交換却下による返金' | admin入力
│       ├── grantedBy: string        ← admin の userId / 'system'（自動付与）
│       ├── relatedThreadId: string  ← 関連イベントID（任意）
│       ├── relatedRequestId: string ← 交換申請関連ID（任意・v7 追加）
│       └── createdAt: timestamp
│
├── shopItems/                     ← ノベルティ商品（v7 で稼働）
│   └── {itemId}/
│       ├── name: string
│       ├── description: string
│       ├── imageUrl: string         ← 当面は外部URL（Storage 有効化後に切替）
│       ├── coinCost: number         ← 必要コイン数（1以上の整数）
│       ├── stock: number            ← -1 = 無制限 / 0 以上 = 残数
│       ├── isActive: boolean        ← false でユーザー側非表示
│       ├── createdAt: timestamp
│       └── updatedAt: timestamp
│
└── exchangeRequests/              ← 交換申請（v7 で稼働）
    └── {requestId}/
        ├── userId: string
        ├── userName: string         ← 非正規化（管理画面表示用）
        ├── itemId: string
        ├── itemName: string         ← 商品スナップショット（商品削除後も表示維持）
        ├── itemImageUrl: string     ← 商品スナップショット
        ├── coinCost: number         ← 申請時の価格スナップショット
        ├── status: "pending" | "approved" | "rejected"
        ├── rejectionReason: string  ← 却下時のみ
        ├── processedBy: string      ← 処理した admin の userId
        ├── processedAt: timestamp
        └── createdAt: timestamp
```

### セキュリティルール方針（Firestore Security Rules）

> Firestore Security Rules は「誰がどのデータを読み書きできるか」のルールです。  
> ※ 現状は暫定ルール（認証ユーザーのみアクセス可）。本格運用ルールは次フェーズで実装。

| コレクション | 読み取り | 書き込み |
|-------------|----------|----------|
| users | 認証ユーザー全員 | 本人のみ（roleはAdmin関数経由） |
| tags | 認証ユーザー全員 | 認証ユーザー（新規タグ追加） |
| threads | 認証ユーザー全員 | 認証ユーザー（作成）/ 本人＋Admin（更新・削除） |
| threads/participations | 認証ユーザー全員 | 本人のみ（自分の申請） |
| coinTransactions | 本人＋Admin | Admin のみ + writeBatch 経由のシステム書き込み |
| shopItems | 認証ユーザー全員 | Admin のみ |
| exchangeRequests | 本人＋Admin | 本人のみ（申請作成）/ Admin のみ（status更新） |
| deletionRequests | 本人＋Admin | 本人のみ（申請作成）/ Admin のみ（status更新） |

### Firestore 複合インデックス対応

`where + orderBy` の組み合わせは複合インデックスが必要。MVP規模では where のみで取得し、クライアント側で sort するパターンを採用：

```js
const q = query(collection(db, 'exchangeRequests'), where('userId', '==', user.uid));
return onSnapshot(q, snap => {
  const list = snap.docs.map(d => ({ id: d.id, ...d.data() }));
  list.sort((a, b) => (b.createdAt?.toMillis?.() || 0) - (a.createdAt?.toMillis?.() || 0));
  setRequests(list);
});
```

この方針は `MyExchangeSection` `MyEventsSection` で採用。スケール時は本番インデックスに切替。

### Firebase Storage 構成（次フェーズで有効化予定）

```
Firebase Storage
├── avatars/{userId}/profile.jpg   ← プロフィールアイコン
└── shop/{itemId}/image.jpg        ← ショップアイテム画像
```

---

## 5. 技術方針

### フェーズ1：Webアプリ MVP
- **フロントエンド**：React（Create React App）
- **バックエンド**：Firebase（Firestore・Authentication・Security Rules / Storage は次フェーズ有効化）
- **ホスティング**：GitHub Pages（連携は次フェーズ）
- **公開URL**：`https://[GitHubユーザー名].github.io/ShareAZ`

### 使用するFirebaseサービス

| サービス | 用途 | 状況 |
|---------|------|------|
| Firebase Authentication | ログイン・サインアップ（メール＋パスワード） | ✅ 稼働中 |
| Cloud Firestore | データ保存（ユーザー・スレッド・コイン・商品など） | ✅ 稼働中 |
| Firebase Storage | 画像ファイル保存（アイコン・ノベルティ画像） | ⏸ 次フェーズ |
| Security Rules | データアクセス制御 | ⚠️ 暫定（次フェーズで本格化） |

### フェーズ1.5：PWA対応
- manifest.json + Service Worker でホーム画面追加に対応

### フェーズ2：スケール対応
- 必要に応じて Next.js + Vercel への移行を検討
- 1,000人規模では Firebase の Spark（無料）プランから Blaze（従量課金）プランへ移行
- 本番インデックスを作成して `where + orderBy` クエリへの切替

---

## 6. デザインシステム

> 詳細は `DESIGN.md` を参照。コーポレートカラーをベースにShareAZ向けにカスタマイズ。

### カラーパレット

| 役割 | カラー |
|------|--------|
| ページ背景 | `#f8f6f2`（温かみのあるオフホワイト） |
| カード背景 | `#ffffff` |
| ブランドカラー・メイン（ネイビー） | `#495e9f` |
| ブランドカラー・メイン（薄いネイビー） | `#eaedf5` |
| ブランドカラー・サブ（レッド / Danger兼用） | `#bd3c40` |
| ブランドカラー・サブ（薄いレッド） | `#fae8e9` |
| AZコイン（ゴールド） | `#f5c518` |
| テキスト（メイン） | `#0f0f0f` |
| テキスト（サブ） | `#757575` |
| ボーダー | `#e0e0e0` |

### タイポグラフィ

- フォント：noto-sans, system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, arial, sans-serif
- 字間：letter-spacing: -0.4px（全体統一）
- 見出しウェイト：600（semibold）
- 本文サイズ：16px / 行間 1.5

---

## 7. 開発・運用環境

| 項目 | 内容 |
|------|------|
| コード管理 | GitHub（リポジトリ未作成） |
| データ | Firebase（プロジェクト作成済み） |
| ホスティング | GitHub Pages（未連携） |
| 開発ツール | Cowork + Macターミナル |
| アプリフォルダ | `~/Documents/App/ShareAZ` |

### 必要なnpmパッケージ

```bash
npm install firebase
npm install gh-pages --save-dev
```

### ファイル構成（v7 時点）

```
ShareAZ/
├── public/
│   ├── index.html
│   ├── manifest.json
│   └── sw.js
├── src/
│   ├── App.jsx           # メインコード（全画面・状態管理、約6,640行）
│   ├── firebase.js       # Firebaseクライアント初期化
│   ├── index.js
│   └── index.css
├── .env                  # Firebase接続情報（GitHubには上げない）
├── firestore.rules
└── package.json
```

### 環境変数（.env）

```
REACT_APP_FIREBASE_API_KEY=...
REACT_APP_FIREBASE_AUTH_DOMAIN=...
REACT_APP_FIREBASE_PROJECT_ID=...
REACT_APP_FIREBASE_STORAGE_BUCKET=...
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=...
REACT_APP_FIREBASE_APP_ID=...
```

---

## 8. 開発フロー（通常）

```bash
# ローカルで確認
cd ~/Documents/App/ShareAZ
npm start

# 本番反映（GitHub Pages にデプロイ・連携後）
git add .
git commit -m "変更内容のメモ"
git push origin main
npm run deploy
```

---

## 9. 将来追加機能メモ

- [ ] **Firebase Storage 有効化**：アバター画像・商品画像のアップロード（次回最優先）
- [ ] **GitHub リポジトリ作成・GitHub Pages デプロイ連携**
- [ ] **Firestore セキュリティルールの本格運用化**
- [ ] **イベント作成の管理者承認制**：新規/告知からの作成いずれも、管理者承認後に公開（`eventCreationRequests/`）
- [ ] コメント編集・削除機能（MVPは投稿のみ）
- [ ] スレッド削除の管理者承認フロー → 既存（v6 で実装済み）
- [ ] 通知機能（イベント参加者へのお知らせ）
- [ ] コイン付与の自動化（イベント完了を自動判定）
- [ ] 初回プロフィール設定+5コインの自動付与
- [ ] ランキング機能（月間コイン獲得ランキング）
- [ ] 部署・チーム単位でのグループ機能
- [ ] メンバー機能フェーズ2：共参加回数表示・チーム機能
- [ ] 管理画面：データ集計タブ（CSV出力・参加率統計）
- [ ] 在庫超過の race condition 対策（`runTransaction` 化）

---

## 10. 未決定事項

- [ ] GitHubアカウント名（リポジトリ作成時に必要）
- [ ] FirebaseプロジェクトのAPIキー・設定（プロジェクト作成済み）
- [ ] ノベルティ商品ラインアップ（品目・価格・在庫数）
- [ ] アバター・商品画像のサイズ制限・許可拡張子（Storage 設計時に決定）

---

## 変更履歴

| バージョン | 日付 | 内容 |
|-----------|------|------|
| v1.0 | 2026-04-24 | 初版作成 |
| v1.1 | 2026-04-24 | バックエンドを Supabase → Firebase に変更。データ設計をFirestore形式に更新 |
| v1.2 | 2026-04-24 | デザインシステムを確定（DESIGN.md）。フォルダパスを ShareAZ に修正 |
| v1.3 | 2026-04-25 | 大型仕様変更：①雑談カテゴリ廃止、②告知→イベントの昇華フロー、③コメント機能、④「興味あり」リアクション、⑤スレッドにタグ機能、⑥タグフィルタはボトムシート方式、⑦ホーム画面で公式お知らせ優先表示、⑧管理画面（Admin専用）追加、⑨マイページに参加履歴・ランクバッジ、⑩メンバー機能拡張、⑪メンバーランク機能、⑫ボトムナビ順序変更 |
| v1.4 | 2026-04-26 | ポイント交換機能の本実装を反映：①ボトムナビ「ショップ」→「ポイント交換」改称、②`shopItems/` `exchangeRequests/` のデータ構造を実装に合わせて確定、③申請時即減算・却下時返金のアトミック処理を仕様化、④管理画面に「ノベルティ」「交換申請」タブ追加（合計7タブ・4列グリッドUI）、⑤マイページに交換申請履歴セクション追加、⑥Firestore複合インデックス回避パターンを明文化、⑦Firebase Storage / GitHub連携 / セキュリティルール本格化を残課題として整理 |
